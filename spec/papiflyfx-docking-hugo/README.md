# Hugo SSG managed website preview

## hugo gemini

I am developing a new JavaFX docking framework, and I need to craft a research document to develop a Hugo docking component that shows a website driven by Hugo SSG

Here is the link to the framework https://github.com/org-metalib/papiflyfx-docking

## hugo chatgpt

I am developing a new JavaFX docking framework, and I need to craft a research document to develop a Hugo docking component that shows a website driven by Hugo SSG

Here is the link to the framework https://github.com/org-metalib/papiflyfx-docking

## hugo grok

I am developing a new javafx docking framework and I need to craft Craft a research document to develop hugo docking component that shows a website driven by [Hugo SSG](https://gohugo.io/)

here is the link to the framework https://github.com/org-metalib/papiflyfx-docking

help with specific docking integration, or research on Hugo CLI automation!

##

- plan-copilot-opus.md
- plan-codex.md

Write a very detailed `spec/papiflyfx-docking-hugo/plan-copilot-codex.md` document outlining how to implement this component.
Include code snippets. Do not include compatibility requirements.

incorporate ideas from the following sources:
- spec/papiflyfx-docking-hugo/hugo-chatgpt.md
- spec/papiflyfx-docking-hugo/hugo-gemini.md
- spec/papiflyfx-docking-hugo/hugo-grok.md

When you process the sources make sure you always apply the following:
- This plan to develop a new docking component: papiflyfx-docking-hugo that is organically could be a part of papiflyfx-docking application.
- This component is designed to show the hugo output using internal javafx web browser
- This component uses hugo cli that should be preinstalled on the system
- The component checks if hugo is preinstalled
- The component can provide a clickable url to show the generated by hugo content with the default system browser
- The component does not include an internal web server
- The component starts and stops hugo server when necessary using hugo cli
- The component targets Java 25 / JavaFX 23.0.1. It does not target backward compatibility (e.g., Java 21 + JavaFX 21/22)
- Native-image target is not required
- The component is implemented based on available javafx components. The component should not bring any dependencies

## Merge hugo plans

```yaml
Base plan: plan-codex.md
```

Add everything missed from spec/papiflyfx-docking-hugo/plan-copilot-opus.md

## Implementation

Implement everything specified by `spec/papiflyfx-docking-hugo/plan-codex1.md`. when you’re done with a task or phase, mark it
as completed in the `spec/papiflyfx-docking-hugo/plan-codex1.md` document.
Add new `spec/papiflyfx-docking-hugo/progress.md` file to track your progress.
Do not stop until all tasks and phases are completed. Do not add unnecessary comments or javadocs, do not use any or unknown types.
Continuously run typecheck to make sure you’re not introducing new issues.

## readme

add hugo module readme

## Color scheme observation

Hugo preview toolbar is not visually appealing. Docking theme should not apply to the hugo dock content.

## Hugo server start/stop buttons accessibility

› when hugo server is started start button should be grayed out. In reverse the stop button should be grayed out when the server stopped

› check accessibility policy for the next/prev, reload and "open in browser"  buttons
